name: Release
on:
  push: # { tags: v* }
  workflow_dispatch:

jobs:
  gem:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
          ruby-version: 3.1
      - run: rake build
